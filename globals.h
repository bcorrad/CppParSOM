#ifndef UNTITLED_GLOBALS_H
#define UNTITLED_GLOBALS_H

constexpr int N_THREADS = 4; // [1, 2, 3, 4]
constexpr int FLOAT_MIN = 0;
constexpr int FLOAT_MAX = 1;
constexpr int WEIGHT_SIZE = 10; // size of weight vector of each node [5, 50, 100].
constexpr int EPOCHS = 100;   //the number of epochs desired for the training.
constexpr double START_LR = 0.001;    //the value of the learning rate at the start of training.
constexpr int ROWS = 10;
constexpr int COLS = 10;
constexpr int N_INPUTS = 1000; // number of weights each node must contain [5, 50, 100].

#endif //UNTITLED_GLOBALS_H
